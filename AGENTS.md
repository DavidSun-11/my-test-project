# Codex 工作规则

修改本仓库前，必须先阅读 `PROJECT_CONTEXT.md`，恢复项目上下文和既有约定。

## 基本原则

- 不重做网站。
- 保留现有功能。
- 保留 Live2D、视频背景、音乐播放器和已有互动模块。
- 优先最小改动，不重构无关代码。
- 默认使用仓库已有代码风格。

## 验收与检查

- 不要自动测试网页。
- 不要截图。
- 不要运行长时间检查。
- 修改完成后直接说明修改内容，由用户自己打开 GitHub Pages 验收。

## Git 规则

- 修改完成后必须 commit 并 push 到 `origin/main`。
- 不要只 commit 到本地 `main`。
- push 成功后告诉用户 commit id。
