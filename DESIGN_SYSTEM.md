# 君雪的个人网站 UI 视觉规范

本文档用于统一君雪个人网站的视觉方向。后续任何页面美化、菜单面板、弹窗或互动功能改版，都应先参考本规范，避免出现默认灰色按钮、临时调试框、文字黏连和布局混乱。

## 网站名称

君雪的个人网站

## 核心风格

- 深蓝星空：页面底色应保留夜空、星光、冰湖或极光氛围，不做纯白后台页。
- 冰蓝玻璃：主要面板、卡片、弹窗使用半透明玻璃感、冰蓝描边和柔和内发光。
- 淡紫柔光：用于高光、hover、selected、按钮渐变和局部氛围，不大面积堆叠。
- 幻想感：可以使用雪晶、星芒、极光、冰晶、柔光线条等轻量装饰。
- 甘雨陪伴感：互动面板应温柔、清晰、像有人陪伴，而不是冷冰冰的工具框。
- 温柔但有高级感：信息要易读，层级要明确，视觉要精致但不能影响功能。

## 主色系统

- 深蓝背景：用于页面底色、弹窗深层背景和夜空氛围。
- 冰蓝描边：用于主面板边框、卡片边框、按钮描边和聚焦状态。
- 淡紫光晕：用于按钮渐变、选中态、状态胶囊和局部氛围。
- 银白文字：用于标题、重要数值和按钮文字，必须保证对比度。
- 少量金色 / 铜色 / 银色：仅用于状态点缀、等级卡、投票卡或强调信息。

## 禁止风格

- 默认浏览器灰色按钮。
- 大块纯黑弹窗。
- 文字和数值黏在一起，例如 `当前账号davidsun`。
- 按钮散落在面板四角。
- 没有层级的裸文本。
- 选项卡像临时调试按钮。
- 手机端横向溢出。
- 文字竖排或被挤压成不可读状态。
- 为了视觉效果破坏现有功能逻辑。

## 页面整体规则

- 所有弹窗必须有清晰结构：header / content / actions / footer。
- 所有卡片必须有清晰标题和副标题，必要时补充状态或说明。
- 所有按钮必须区分主按钮、次按钮、返回按钮。
- 所有菜单、弹窗和卡片必须适配手机端。
- 所有信息项必须 label 和 value 分离，不能把字段和值直接拼在一起。
- 所有列表、选项、投票项都应使用卡片式结构，而不是裸按钮堆叠。
- 所有状态提示必须有明确语义，例如进行中、已结束、加载失败、未登录。
- 不允许为了美观改动 Supabase、支付、音乐、天气、Live2D、老板评价等业务逻辑。

## 典型组件风格

### 冰蓝玻璃卡片

- 半透明深蓝到冰蓝渐变背景。
- 冰蓝或淡紫边框。
- 轻微 `backdrop-filter` 和 `-webkit-backdrop-filter`。
- 外发光克制，内阴影增强层级。
- 圆角柔和，避免生硬矩形。

### 圆角发光按钮

- 主按钮使用冰蓝到淡紫渐变。
- 次按钮使用半透明深蓝玻璃底。
- 返回按钮弱化，但仍清楚可点。
- hover / active 只做轻微发光、位移或亮度变化。
- disabled 不能变成浏览器默认灰色。

### 半透明信息卡

- 用于当前账号、当前状态、当前选择、统计数值等。
- label 使用较小字号和低亮度。
- value 使用更高对比度和更明显字号。
- 每项之间留足间距。

### 分色投票卡

- 投票选项必须是完整卡片，不是普通按钮。
- 每张卡包含徽章、标题、人数、选中状态。
- 铜牌、银牌、金牌、顶级、无等状态可以有不同色彩点缀。
- selected 状态必须明显，但不能遮挡文字。

### 轻量状态胶囊

- 用于进行中、已结束、暂未开启、加载中、失败等状态。
- 胶囊文字要短，颜色要和状态匹配。
- 不要把状态文案散落在面板角落。

### 柔和 hover / selected 状态

- hover 只增强亮度、边框和轻微发光。
- selected 必须比 hover 更明确。
- 手机端不能依赖 hover 才能理解状态。

## 手机端原则

- 主容器宽度优先使用 `92vw` 或 `calc(100vw - 32px)`。
- 不允许横向滚动。
- 面板内容过多时内部滚动。
- 文字不能竖排。
- 点击区域不小于 40px。
- 甘雨静态卡、Live2D 按钮、菜单和底部入口不能互相遮挡。

## 修改 UI 前的底线

任何 UI 修改都必须先确认：

- 是否保留现有业务逻辑。
- 是否保留原有 DOM id/class 中被 JS 依赖的部分。
- 是否手机端可用。
- 是否避免默认按钮和临时调试框。
- 是否更新必要缓存版本号。
- 是否通过 `git diff --check`。
