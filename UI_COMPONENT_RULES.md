# UI 组件实现规则

本文档用于约束君雪个人网站的具体组件实现。后续 CodeX 修改 UI 前，应同时参考 `DESIGN_SYSTEM.md` 和本文档。

## A. 弹窗规则

- 外层必须是玻璃面板，不能是默认黑框或纯灰框。
- 必须有标题区，包含主标题和必要的副标题或状态胶囊。
- 必须有内容区，承载表单、卡片、结果或说明。
- 必须有操作区，统一摆放主按钮、次按钮和返回按钮。
- 必须有关闭按钮，位置固定且不会遮挡标题。
- 内容较多时，弹窗内部滚动，不把整个页面挤出屏幕。
- 弹窗不能超出手机屏幕宽度，不能造成横向滚动。
- 信息项必须 label 和 value 分开，不能出现文字和值黏连。

## B. 菜单规则

- 主菜单按钮必须统一高度、圆角、边框和间距。
- 二级菜单必须有返回按钮。
- 菜单按钮不能像默认灰色按钮。
- 菜单按钮需要有清晰主文案和可选副标题。
- 手机端按钮不能横向溢出，不能被挤成竖排文字。
- 菜单打开时，应避免和粉色对白框、甘雨静态卡、Live2D 控件重叠。
- 不要复制一套新菜单系统，优先复用现有菜单渲染逻辑。

## C. 投票卡规则

- 投票选项必须是卡片，不是普通按钮。
- 每张投票卡至少包含：
  - 徽章
  - 标题
  - 人数
  - 选中状态
- 不同选项可以使用不同色彩点缀，但文字必须清楚可读。
- 选中状态必须明显，例如更亮边框、发光、`已选择` 标识。
- disabled 状态不能变成浏览器默认灰。
- 投票结束后，卡片仍应保持玻璃质感，只取消可点击表现。
- 投票卡布局必须适配手机端，不能横向撑破屏幕。

## D. 操作按钮规则

- 主按钮更亮，用于提交、开启、确认、查看等关键动作。
- 次按钮中等强调，用于刷新、重试、辅助操作。
- 返回按钮弱化，但必须清楚可点。
- 关闭按钮使用圆形或圆角描边样式，不使用裸文本。
- 按钮必须整齐排列在统一 actions 区域。
- 按钮不能散落在面板角落或夹在内容中间。
- 多按钮时允许换行，但高度、圆角和间距要统一。
- 移动端按钮点击区域不小于 40px。

## E. 手机端规则

- 主面板 `max-width` 优先使用 `92vw` 或 `calc(100vw - 32px)`。
- 不允许横向滚动。
- 文字不能竖排。
- 卡片和按钮不能贴边。
- 面板内容过多时内部滚动。
- 点击区域不小于 40px。
- 图片、卡片、Live2D iframe、静态甘雨卡都要做 viewport clamp。
- 底部入口、显示甘雨按钮、菜单弹窗不能互相遮挡。

## F. CodeX 修改 UI 前检查清单

每次 UI 修改前必须检查：

- 是否遵守 `DESIGN_SYSTEM.md`。
- 是否复用了统一组件规则。
- 是否避免默认按钮样式。
- 是否没有改业务逻辑。
- 是否保留现有 JS 依赖的 id、class、data 属性。
- 是否手机端可用。
- 是否不横向溢出。
- 是否没有用户可见调试文案。
- 是否更新必要缓存版本号。
- 是否执行 `git diff --check`。
- 是否对修改过的 JS 文件执行 `node --check`。

## G. 不应做的事

- 不要为了视觉重写业务逻辑。
- 不要新增外部图标库、字体库或 CDN。
- 不要把按钮做成默认灰色。
- 不要把临时调试文字留给用户看。
- 不要把状态、数值和 label 直接拼接。
- 不要让投票卡、菜单项或表单项像调试按钮。
- 不要在手机端产生横向滚动。
