# Miku-style Live2D model

把你下载并确认授权可用的蓝绿色双马尾 Live2D Web 模型放在这个目录。

脚本默认读取：

```text
live2d/models/miku-style/model.json
```

上传模型后，这个目录通常应该包含：

```text
model.json
*.moc 或 *.moc3
textures/
motions/ 或 expressions/
physics.json 或 *.physics3.json
```
